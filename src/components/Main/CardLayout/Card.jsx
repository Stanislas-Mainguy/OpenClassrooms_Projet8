import React from "react";
import CardTitle from "./CardTitle";

const Cards = () => {
    return (
        <div className="cards">
            <CardTitle />
        </div>
    );
};

export default Cards;